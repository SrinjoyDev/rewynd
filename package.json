{
  "name": "rewynd-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "rewynd — a zero-config, OTLP-native flight recorder for your backend (TUI + CLI + MCP).",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@11.0.8",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
